<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android" >
    <!-- 实心 -->
    <solid android:color="@color/titlebgcolor" />
    <!-- 边框-->
    <stroke
        android:width="0.5dp"
        android:color="@color/sbg" />

    <!-- 圆角 -->
    <corners android:radius="60dp"

        />
    <!-- 边距   -->
    <padding
        android:bottom="10dp"
        android:left="60dp"
        android:right="60dp"
        android:top="10dp" />

</shape>